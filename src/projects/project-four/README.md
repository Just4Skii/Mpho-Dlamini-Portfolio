# Project Four (Reserved Slot)

This slot is reserved for future independent frontend products.

To add a new project:
1. Create `src/projects/[project-name]/`
2. Add the project root component and wrapper
3. Add the project entry in `src/config/projects.ts`
4. Register the route in `src/App.tsx`
